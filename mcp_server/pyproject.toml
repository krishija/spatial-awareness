[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spatial-mcp"
version = "0.1.0"
description = "MCP server scaffold for Owkin K Pro — spatial exhaustion tools + memory"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]>=1.10,<2",
  "jsonschema>=4.20",
  "boto3>=1.35",
  "requests>=2.31",
<<<<<<< Updated upstream
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
=======
  "pandas>=2.0",
  "pyarrow>=14",
>>>>>>> Stashed changes
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
spatial-mcp = "spatial_mcp.server:main"
spatial-agent = "spatial_mcp.agent.cli:main"
spatial-api = "spatial_mcp.http_api:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
spatial_mcp = ["py.typed"]
